# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.python-version
.venv/
venv/

# Generated experiment artifacts (re-derivable from code + raw data)
*.csv
*.json
*.jsonl
*.json.gz
*.jsonl.gz
*.parquet
*.pkl
*.npz

# Generated configs (use generate_*_configs.py to recreate)
**/configs/
**/inputs/
*.yaml
!**/sbatch/*.yaml

# Logs and run outputs
logs/
runs/
slurm-*.out
slurm-*.err
*.log
*.tsv

# Model checkpoints
*.pt
*.pth
*.bin
*.safetensors
checkpoints/

# Documentation drafts (kept locally only; canonical paper-design doc is not uploaded)
*.md
!README.md
# Exception: the frozen lock-time snapshot is git-tracked as a GitHub-visible
# reference for reviewers (see paper_development_design.md Appendix B row 3,
# 2026-04-16). The evolving paper_development_design.md remains ignored.
!paper_development_design_locked_v1.md
# Exception: the paper-ready scientific skeleton extracted at lock v2
# (2026-04-27). Tracked so the prelock_v2 tag has a complete reproducibility
# package on GitHub.
!paper_methods_draft.md

# ID lists and submission records (regenerable)
phase_*_ids.txt
phase_*_submission_record.json
phase_*_eval_submission.json

# Cached data (PubMed, HuggingFace)
pubmed_cache/
hf_hub_cache/
.cache/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
